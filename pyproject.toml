[project]
name = "directory-reconciliation-agent"
version = "0.1.0"
description = "Prototype for a provider-directory reconciliation agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[tool.ruff]
line-length = 120
